// Fully client-rendered SPA (matches the original CRA single-page app). Disabling
// SSR avoids server-rendering the heavily animation/DOM-driven landing sections.
export const ssr = false;
export const prerender = false;
